#include "vcMainWindow.h"
#include "VulkanWidget.h"


//vc::MainWindow::MainWindow(VulkanWindow* vkWin)
//{
//	m_vkWin = nullptr; // using it from somewhere else
//	QWidget* wrapper = QWidget::createWindowContainer(vkWin);
//}

vc::MainWindow::~MainWindow()
{
	//if (m_vkWin != nullptr)
	//{
	//	delete m_vkWin;
	//	m_vkWin = nullptr;
	//}
}

vc::MainWindow::MainWindow()
{
	//m_vkWin = new VulkanWindow();
	//QWidget* wrapper = QWidget::createWindowContainer(m_vkWin);
}
